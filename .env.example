# Anthropic API key for the semantic-validation layer (Claude Opus 4.7).
# Sin esta clave la capa AI queda deshabilitada y el sistema sólo aplica las
# capas estáticas — NO recomendado en producción.
ANTHROPIC_API_KEY=sk-ant-...

# Puerto del servidor HTTP (opcional, default 3000).
PORT=3000
